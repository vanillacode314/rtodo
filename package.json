{
  "name": "rtodo",
  "private": true,
  "scripts": {
    "dev": "turbo dev --filter='./apps/*'",
    "build": "turbo build",
    "format": "turbo format",
    "lint": "turbo lint",
    "test": "turbo test"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "packageManager": "bun@1.2.5",
  "devDependencies": {
    "turbo": "^2.4.4"
  },
  "dependencies": {
    "@lukemorales/query-key-factory": "^1.3.4"
  }
}
